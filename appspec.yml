#code deploy가 참조하는 배포 설명서 - appspec.yml

version: 0.0
os: linux
files:
  - source: /
    destination: /home /ec2-user/bbok-deploy/ # 배포 파일이 저장되는 경로
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ec2-user
    group: ec2-user